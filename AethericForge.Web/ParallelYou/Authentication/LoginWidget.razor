@inject NavigationManager Navigation

<div class="forge-login">
    <AuthorizeView>
        <Authorized Context="authenticationState">
            <span class="forge-login__identity">
                @authenticationState.User.Identity?.Name
            </span>
            <form method="post" action="/account/logout" data-enhance="false">
                <AntiforgeryToken />
                <input type="hidden" name="returnUrl" value="@ReturnUrl" />
                <button type="submit" class="btn btn-outline-secondary btn-sm">Log out</button>
            </form>
        </Authorized>
        <NotAuthorized>
            <form method="get" action="/account/login" data-enhance="false">
                <input type="hidden" name="returnUrl" value="@ReturnUrl" />
                <button type="submit" class="btn btn-primary btn-sm">Log in</button>
            </form>
        </NotAuthorized>
    </AuthorizeView>
</div>

@code {
    private string ReturnUrl
    {
        get
        {
            var relativeUrl = Navigation.ToBaseRelativePath(Navigation.Uri);
            return string.IsNullOrEmpty(relativeUrl) ? "/" : $"/{relativeUrl}";
        }
    }
}
